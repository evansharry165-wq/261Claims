# Prompt 2 — education alignment, insights cards, rename — push pack contents

`insights_embed.js` was deleted-as-renamed to `education_content.js` — nothing to upload for the deletion, just don't re-upload the old filename.

## Files to upload (overwrite existing)

- **education.html** — P1: Medical Emergency row updated to the LE v TAP (C-74/19) DEFEND position; Natural Disaster row enriched with VAAC/DT-13 evidence detail (no duplicate row added — see report for why); new cascade-explainer callout added below the disruption-type table, reusing the existing "Application note" callout pattern (no new CSS). Code comment added at the top of the disruption-type section per spec. P5: script tag updated from `insights_embed.js` to `education_content.js?v=sessD1`.
- **education_content.js** — P5: renamed from `insights_embed.js`. Header comment rewritten to accurately describe its real, shared purpose (embed-mode CSS + redirects for both `module6-mi.html` and `education.html` — not education-specific despite the new name). No internal module-name export existed to rename (bare IIFE, no `window.X =`).
- **module6-mi.html** — P5: script tag updated to `education_content.js?v=sessD1`. P2.3: fixed the `accent is not defined` / `secondary is not defined` bug in `buildOverviewContent()` — the Cat A/B legend swatches referenced `accent`/`secondary` without either being declared in that function's scope (only declared in two separate, later chart-building functions). Added local `const accent = '#1B3A6B'; const secondary = '#6B6B80';` matching the values used identically elsewhere in the file. Grepped the rest of the file for the same pattern — no other occurrences found.
- **insights.html** — P2.1: wired up the previously-dormant `computeCorrelations()`/`renderCorrelationCards()` into a new "Portfolio insights" section on the intelligence tab, positioned above "Live search & filter", using the existing `.corr-grid`/`.corr-card` CSS (already complete — no new classes needed). P2.2: added the two new cards (`cascade`, `proactive`) to `computeCorrelations()`, reading raw `CaseFiling.listCases()` data directly since the tab's normal case list (`getIntelligenceCases()`) strips `.meta` and excludes the seeded Etna case's evidence/rotation data. Added `stage` as a new supported preset-filter key (additive, per spec) to `intelFilters` and `filterIntelligenceCases()`. **Also fixed a pre-existing, unrelated bug found during verification**: `applyInsightFilter()` — the function that actually renders the visible case sidebar — only ever read live DOM dropdown values and completely ignored `intelFilters`, so `applyCorrelationPreset()` (which every correlation card's "View cases →" button calls) was setting state that nothing downstream consulted; `filterIntelligenceCases()`, which does read `intelFilters` correctly, had zero callers anywhere. This meant "View cases →" has never worked for any correlation card, old or new. Fixed by having `applyInsightFilter()` merge dropdown selections into `intelFilters` (dropdown overrides when set, otherwise preserves whatever a preset already wrote) and call `filterIntelligenceCases()` for the actual filtering, removing the dead duplicate inline logic. Also bumped the reporting-tab iframe's `src` to `module6-mi.html?embed=1&v=sessD1` so the accent-bug fix isn't masked by a cached iframe load. **Post-review fix**: Card 2's finding text originally described a different metric (DIO capturedBy evidence-item attribution, "5 of 12 items") than its filter target (`stage:'inbox'`, landing on 2 unrelated engine-shell cases) — rewritten so the finding, metric, and preset all describe the same population (count of `stage==='inbox'` cases), confirmed the click-through case count now matches the card's metric exactly (2 = 2).

Not touched: `insights_data.js` (correctly untouched per explicit instruction), `defendable_analyser_v3.html`/`defendable_trees.js`/`defendable_registry.js` (prior session's work, not in this session's scope).
