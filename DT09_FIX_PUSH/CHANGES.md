DT-09 restructured to mirror DT-10's defend-by-default pattern (LE v TAP C-74/19 + Wallentin-Hermann + ANO/ICAO anchor, concede only on foreseeable/known-before-boarding); `DT9_MEDICAL_EC` registered in `defendable_registry.js`; `defendable_analyser_v3.html`'s script-tag versions for both files bumped (`?v=sessC2`/`?v=sessC4`) so the fix isn't masked by stale browser caching as it was last time.
