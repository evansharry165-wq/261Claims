# Session B — DIO ↔ Lawyer connective tissue — push pack contents

No PAT configured for this session (same as Session A) — drag-drop these files into the GitHub web UI, overwriting the existing files of the same name at the **repository root**.

Two of these six files (`dio.html`, `dio-case.html`) also still carry Session A's already-uploaded changes, since Session A's edits were never committed to local git (only uploaded by hand to GitHub) — the local working copy has carried them forward since. Uploading these files again is safe and non-destructive: it re-applies the same Session A content you already have live, plus this session's new changes on top. Nothing from Session A is reverted.

## Files to upload (overwrite existing)

- **case_filing.js** — Root-cause data fix + attribution data. Added `dep:'GVA'`, `arr:'LGW'` to the `DEF-DEMO-ETNA-CASCADE` case object (it previously only had a combined `route` string, which is why Emma's Territory pulse and the case's own "Route" fact row both read blank/zero — confirmed live before the fix). Added a new `capturedBy: 'EH'` and `captureMethod: 'api-pull'` field to the 5 seeded Etna evidence items (distinct from `attachedBy`, which stays `'SB'` — Sarah genuinely attached these to the case; Emma is who originally captured them into the DIO pool). Also added `capturedBy`/`captureMethod` pass-through to the shared `seedEvidenceAttachments()` helper so the new fields aren't silently dropped (they default to `null` everywhere else — this is what gives the "no attribution on Hartley's older items" fallback something real to demonstrate, not a hardcoded exception). **Audit-chain entries were deliberately left untouched** — `actor` stays `'SB'` on all 5, no hash recomputation, chain still verifies `{ok:true, entries:5, breaks:[]}`.
- **case_evidence_repository_ui.js** — B.1: attached evidence items in the Repository tab now show a quiet "· attached by Emma Hughes" line (reading the new `capturedBy` field, resolved against `USERS` to confirm it's a real DIO), styled identically to the existing Note-line metadata — text only, no chip, renders nothing when absent. B.2: a one-line provenance summary now sits below the attached list ("5 items contributed to this case by Emma Hughes · Documentary Intelligence Officer, UK + IE — view Emma's activity on this case"), with the link opening a small read-only modal (shell borrowed from `case_evidence_fetch_ui.js`'s existing `.cef-modal` pattern, close-button borrowed from `dio-case.html`'s existing `.dio-req-close` X pattern) listing the DIO's contributed items with timestamp + capture method. In building this, found and fixed a latent bug in the file's `USERS` lookup pattern (`global.USERS` is always `undefined` since `USERS` is declared `const` at top level in `shared_data.js` and never attaches to `window` — the working pattern, already used correctly in `dio_core.js`, is a bare `USERS` reference). Fixed it in the two new functions that needed it; left the pre-existing `_isDIOActor()` untouched since its only caller path (DIO viewers on `case.html`) is unreachable in practice — DIO users are redirected away from `case.html` before this code runs.
- **dio.html** — B.3: Emma's live incident feed now includes a completed-capture entry for the Etna event ("Mount Etna eruption — 5 evidence artefacts captured (VAAC, NOTAM, ATFM, METAR ×2). Fed to case DEF-DEMO-ETNA-CASCADE."), styled with the existing feed-item template and a new muted `.feed-src-dio` chip (same structure as the other three source chips, neutral-grey tokens already used elsewhere in the app for a "no-hit" state — none of the four existing source chips honestly represent an internal DIO-capture fact, so this is the smallest new one). Scoped to territories that actually cover LGW (the case's real destination) rather than the generic text-match filter, since the item isn't a syndicated article with geography in its copy. Territory pulse counters needed no code changes — they now read correctly once the case_filing.js dep/arr fix lands, verified live: Territory cases 0→1, Hot airports stays 0 (honestly — LGW's 1 case is below the ≥3 threshold; the other three counters were also unaffected). Also bumped the unversioned `case_filing.js` script tag to `?v=sessB1` — the page was serving a stale cached copy of that file during testing, same staleness class Session A's Fix 4 already fixed for `nav.js`.
- **case.html** — Bumped `case_evidence_repository_ui.js` from `?v=peb5` to `?v=sessB1` and added a version tag to the previously-unversioned `case_filing.js` (`?v=sessB1`), for the same staleness reason as above — this page hosts the Repository tab where B.1/B.2 live. Also added a `dio_territory.js?v=sessB1` script include, which wasn't loaded on this page at all before — needed so the B.2 provenance line can show the DIO's territory label ("Documentary Intelligence Officer, UK + IE") on the solicitor's own case view, not just on DIO-side pages that already had it.
- **dio-case.html** — Bumped `case_filing.js` from `?v=sessA1` to `?v=sessB1` (content changed this session, same staleness reasoning).
- **repository.html** — Added a version tag to the previously-unversioned `case_filing.js` (`?v=sessB1`) — this page also mounts the Repository panel.

## Verification performed (live, against a local static server)

- B.1: attribution renders on all 5 seeded Etna items, tooltip shows role + timestamp, visually matches the existing Note-line weight (confirmed via screenshot). Hartley's 4 items (`DEF-2026-EW-0089`) render with zero attribution lines and no provenance summary — confirmed via DOM query, not just visual absence.
- B.2: provenance line renders with exact copy, link opens the read-only modal (5 items, timestamp + "API pull" each, no attach/detach buttons), X-close and backdrop-click both dismiss it.
- B.3: feed entry renders for Emma (EH — territory covers LGW) with the muted DIO CAPTURE chip; confirmed **absent** for Françoise (FD — France territory) as a regression check. Territory pulse: Territory cases 1, Hot airports 0, Open req 0, Engine intake 0 — matches the design doc's honestly-computed numbers exactly.
- `CaseAuditTrail.verify('DEF-DEMO-ETNA-CASCADE')` → `{ok:true, entries:5, breaks:[]}`, confirming the untouched audit chain still passes.
- No console errors introduced (checked on both `case.html` and `dio.html`; the only console entries present are pre-existing 404s from the offline sandbox not reaching the real `evidence-collection` endpoint, unrelated to this session).

Not touched, per explicit scope: `defendable_analyser.html` (v2), repo root layout, and no re-review of Session A's own fixes.
